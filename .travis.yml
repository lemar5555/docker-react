sudo: required
services:
    - docker

before_install:
    - docker build -t lemar555/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true lemar555/docker-react npm run test -- --coverage